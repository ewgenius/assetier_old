{
  "name": "@assetier/prisma",
  "version": "0.0.0",
  "main": "index.ts",
  "types": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "npm run prisma:generate && npm run prisma:push",
    "prisma:generate": "npx prisma generate",
    "prisma:push": "npx prisma push"
  },
  "dependencies": {
    "@prisma/client": "^3.7.0"
  },
  "devDependencies": {
    "@assetier/config": "workspace:*",
    "@assetier/tsconfig": "workspace:*",
    "prisma": "^4.0.0",
    "typescript": "^4.5.3"
  }
}
